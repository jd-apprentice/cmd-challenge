# A process on this computer managed to delete all of the files in the /bin and /usr/bin directories!
# The good news is that you still have a shell prompt, the bad news is that you are only able to execute one command to complete each challenge.
# For now, all you need to do is figure out where you are, print the current working directory.

pwd